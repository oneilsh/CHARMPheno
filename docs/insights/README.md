# Modeling & Experimental Insights

This directory captures *learned things about the modeling regime or the data*
that aren't recoverable by reading the code. Insights complement ADRs:

- **ADRs** (`docs/decisions/`) record *why we chose X over Y* — architectural
  and organizational decisions. Forward-looking, prescriptive.
- **Insights** (`docs/insights/`) record *what we observed when we tried
  things* — empirical findings about how models behave on this data, which
  diagnostics actually discriminate, which failure modes are real, which
  hypotheses didn't survive contact with reality. Backward-looking,
  descriptive.

When a new modeling phenomenon, failure mode, or counterintuitive result
emerges from a run, add an insight here with the next four-digit number.
Existing insights can be marked **Refuted by NNNN** in their header when a
later run overturns them — don't delete; the trajectory of what we believed
and when is itself useful.

## Format skeleton

    # NNNN — Short Title
    **Date:** YYYY-MM-DD
    **Topic:** hdp | lda | doc-units | diagnostics | svi | ops | npmi
    **Status:** Observed | Confirmed | Tentative | Refuted by NNNN

    [Narrative body — typically 100–400 words. Subsections OK if useful
    (Observation / Interpretation / Implications), but optional.]

    **Setting context:** One short paragraph naming the run setup that
    revealed this — model, doc-unit, K/T, key hyperparameters that differ
    from recent defaults. Detail level: "trying with very large K values
    revealed X; other settings as in other recent patient-year LDA runs"
    is enough. The point is so a future reader knows whether the
    observation likely generalizes or was specific to that regime.

## Status meanings

- **Observed**: seen in one run; could be regime-specific. Default for new
  insights.
- **Confirmed**: reproduced across ≥2 distinct runs (different seeds, doc
  units, or hyperparameter neighborhoods).
- **Tentative**: noticed but with confounders that prevent ruling out
  alternative explanations.
- **Refuted by NNNN**: a later insight contradicts this one; keep the entry
  but link forward.

## What does NOT go here

- Facts about how the code works → code comments / docstrings.
- Architectural choices → ADRs in `docs/decisions/`.
- Coding conventions or agent-collaboration rules → AGENTS.md.
- Run logs and outputs → not committed; insights distill from runs but
  don't replicate their raw output.

## Index

(Append entries here as new insights are written, newest at top.)

- [0079](0079-gated-pc-topics-starve-at-a-hard-depth-5-cliff-label-coverage-is-not-topic-learnability.md) — The gated-PC per-node topics starve at a **hard cliff at depth 5**: topic evidence (λ pseudo-count mass) falls ~200× from depth 4 (~1e3) to depth 5 (~5.15 = the Dirichlet prior floor), and depths 5–16 (2531 nodes, 93% of the label space) sit at the flat prior — only the top 4 Mondo levels (182 nodes) are actually fit, 73% of node topics are near-prior, 8 background topics absorb the corpus. Words confirm it (starved topics' top concepts are corpus-marginal common codes / normal labs, no disease signal). Mechanism: closure-gated allocation + label stripping means a leaf's own block gets only tokens its ancestors don't already explain (≈ nothing at depth). So **label coverage ≠ topic learnability** — orthogonal to 0078 (episode anchoring un-starves the *label* space); the readout survives (AUC 0.72) only by decoding deep nodes through *ancestor*/background topics, which is why cond_AUC slides with depth. Episode anchoring can't fix a depth-5 cliff; judge 0111-vs-0112 on deep-node evidence. Read from the 0111 episode fit's saved globals via `inspect_topics.py` (0112 not yet fit).
- [0078](0078-first-episodes-die-to-the-prior-obs-gate-while-episode-anchoring-unstarves-the-label-space.md) — The 365d prior-obs gate kills 66.2% of FIRST diagnostic episodes vs ~15% of later ones — incidence-anticorrelated censoring, measured (`diag-episode-probe` on the 0110 E4 sidecar, 345k persons, the assembler's own gate called): "a year of history before onset" is exactly what patients presenting at onset don't have. Meanwhile episode anchoring un-starves the label space: 2,583/2,714 Mondo nodes reach ≥20 gated onset episodes (2,321 ≥100, frontier lower bound) vs 0110's 923 starved — the starvation was the one-random-year index looking away from onsets, not disease rarity. Episodes are heavy-tailed (gated ~8.5–10.4/person, p99 36–45; uncapped ×8.6 infeasible), so per-person caps do the sizing (cap 3 → ×2.66, cap 5 → ×3.98) and gap 90d beats 60d (fewer, denser episodes, same yield). Probe-before-plan paid off: the numbers moved distributed eval from contingency to precondition (evaluation, corpus-construction, episodes, incident, exp-0111)
- [0077](0077-widening-the-label-window-dilutes-auc-while-ap-rises-as-a-base-rate-artifact.md) — Widening the label window DILUTES discrimination — shared-set AUC falls monotonically 0.6003→0.5899→0.5857 at W=1/2/3y (0110 saved fit + sidecar relabeling, zero re-fits) — while AP rises 0.020→0.037 as a pure base-rate artifact (prevalence climbs with W; AP follows at fixed ranking). The pre-registered rule (rising AUC ⇒ widen) was NOT triggered; window-widening is refuted as a training-target change and demoted to a free eval-side reporting axis. Reconciles with insight 0076's GROWING decile gradient: ranking of eventual converters holds up while added late converters are individually harder to rank. Never scope an experiment off AP movement without checking whether prevalence moved with it (evaluation, label-windows, metrics, scoping)
- [0076](0076-prospective-within-retrospective-validation-pu-contamination-concentrates-where-the-model-points.md) — Prospective-within-retrospective case-finding validation, no chart review: the pooled PU floor (incident negatives converting at 0.96/1.77/2.53% by 1/2/3y) is alone indistinguishable from background incidence, but stratified by the model's own score deciles the conversion rate climbs MONOTONICALLY, top ≈3× bottom at every horizon (+0.014/+0.024/+0.032) — background incidence would be flat, so the label-noise channel concentrates exactly where the model points: the contamination IS the model surfacing not-yet-diagnosed cases (a 3y top-decile "negative" carries ~1-in-20 future-diagnosis odds vs ~1-in-60 bottom). Prerequisites are two cheap persisted artifacts: a full-record first-attestation sidecar and saved scoring heads. Lower bound (channel 1 only); validates ranking, not calibrated risk (evaluation, pu-learning, case-finding, label-noise)
- [0075](0075-incident-masking-prices-tracking-vs-prediction-at-0067-auc.md) — Incident masking prices the tracking-vs-prediction gap: same model, same test docs, shared 1,599 nodes — prevalent 0.7412 AUC vs incident 0.6741 (exp 0110). ~0.067 AUC of apparent skill was the model reading already-charted diagnoses back; 0.6741 is the honest forward-prediction headline, and every prevalent-only number in prior experiments carries the inflation. The AP halving (0.436→0.243) is prevalence-mechanical, NOT a second skill loss — incident cohorts drop the bulk of positives, and AP follows base rate where AUC doesn't. The 923 nodes skipped as <20 incident positives are a finding, not bookkeeping: a population-random index starves a third of the DAG, which is what redirected exp 0111 to episodes (insight 0078). Name claims per D7: "a prevalent-fit model evaluated on an incident cohort" (evaluation, incident, case-finding, metrics)

- [0071](0071-whole-mondo-dag-is-k3800-decomposes-by-body-system-into-a-cascade.md) — The whole-Mondo powered label DAG is K≈3,800 (9,164 anchors → 2,513 powered ≥100 patients + 1,306 branch-point class nodes; exp 0088), ~38× the biggest fit (K=101). The class tree is clinically clean — cardiovascular/nervous/respiratory/endocrine/immune/… system disorders — far cleaner than the SNOMED-derived DAG (which gave degenerate SLE→SLE nodes, insight 0069), vindicating Mondo-over-SNOMED. As ONE fit K≈3,800 is NOT viable, and the binding constraint is specifically the unified co-fit ridge-Newton head (ADR 0043): O(K³·C) compute, O(C·K²) memory ≈ 850 GB Fisher stack. The topic model and the two-stage readout LR both scale; the unified Newton head does not — a scale-dependent tension (unified head at small/medium K, two-stage/staging at whole-Mondo K). CORRECTED same-day: the wall is the DENSE head, not model size — the better fix is a LOCALIZED DAG-closure head in ONE co-fit (each node reads only its gated block + ancestors, ~O(depth) dims → O(C·depth³) total), keeping global topic sharing; the body-system cascade is a fallback / inference-time decomposition, not the required training architecture (case-finding, ontology, mondo, scale, architecture, prediction-constrained)
- [0070](0070-whole-mondo-is-a-near-complete-disease-backbone-unplaced-are-hpo-phenotypes.md) — Whole-Mondo → OMOP places 97.88% of CODED AoU patients (54.66% of all; 626k persons, exp 0087) via the SNOMED-climb ancestor roll-up, despite only 8.2% of the 112k standard SNOMED Condition concepts carrying a DIRECT Mondo xref (the 8.2% is a red herring — granular SNOMED variants roll up to mapped disease ancestors). Mondo is a near-complete disease backbone; the 760-rare-seed placed only 11.71% (exp 0086), so whole-Mondo is the lever. The 1.18% coded-but-UNPLACED are CORRECTLY unplaced: they are HPO-domain phenotypes/findings (Chronic pain, Fatigue, Chest pain, Dyspnea, Tachycardia, "Worried well"), which Mondo (a DISEASE ontology) rightly excludes. No signal lost — finding codes are Condition-domain and remain input TOKENS (feed θ); they just don't get a disease NODE (feature vs label, both correct). Refinement: split the residual into 44.15% no-code (healthy/undocumented) vs 1.18% symptom-only (symptomatic-UNDIAGNOSED = the case-finding target), a cohort-definition change not a modeling one. No HPO-as-node build needed (case-finding, ontology, mondo, snomed, hpo, cohort-definition)
- [0069](0069-ridge-bounded-cofit-head-is-calibrated-at-scale-unified-conditional-model-works.md) — The ridge-bounded co-fit head is ITSELF well-calibrated at 41-anchor scale: a single unified model emits P(child|parent) with no post-hoc fit at pooled ECE 0.0098 (BETTER than the two-stage readout LR's 0.0119) and within ~0.01–0.02 AUC of it (exp 0082, head_l2=0.01, head_penalty=none, |w|max=2126). The small discrimination tax concentrates in the smallest cohorts (Amyloidosis n=66: readout bal_acc 0.644 vs unified 0.534) — the leverage story in reverse: large-n nodes match/edge the readout, small-n lose. The ridge suffices where Firth failed because a fixed absolute L2 is leverage-independent while Firth's pull ∝ p/n vanishes at large n / rank-deficient design (exp 0080). The rare6 pessimism (|w| 1.3e4, garbage calibration, exp 0079) was mis-anchored on the small-sample regime; scale moved us TOWARD the good regime. Motivates ADR 0043 (drop Firth). Caveat: pooled ECE ≠ per-node reliability (next run). Residual small-cohort tax → hierarchical shrinkage, not Firth (svi, pc, case-finding, calibration, prediction-constrained, unified-head, decision)
- [0068](0068-pc-head-gap-was-l2-miscalibration-not-jointness-alternating-reaches-reference.md) — The co-fit head's topics-quality gap (online topics-LR ~0.53 vs reference ~0.87) was NOT joint-vs-alternating and NOT pi-iters/weight_y. Adding fit_mode='alternating' to the reference (block-coordinate L-BFGS, everything else identical to joint) reaches topics-LR 0.874 == joint's 0.862 — alternation does not collapse. The real cause: head_l2 was applied per-doc x n_docs, making head_l2=1e-3 act like Hughes lambda_w~0.84 (~840x too strong), throttling |w| to ~5. Fix = ABSOLUTE head_l2 (= lambda_w, the ridge on the corpus-summed head gradient); at head_l2=1e-3 the online Newton head shapes to topics-LR ~0.96 (>= reference) with |w| finite, no joint step, Newton retained. Corrects 0067's residual-gap attribution; recorded as ADR 0041 (svi, pc, head-optimizer, prediction-constrained, joint-vs-alternating, calibration)
- [0067](0067-pc-head-blowup-is-vanishing-relative-ridge-fixed-l2-fixes-it-inner-loop-redundant.md) — The co-fit head's runaway |w| (3.4e11, head AUC 0.646 while topics-LR 0.965) is a VANISHING RELATIVE RIDGE: PC shaping makes topics separable, p(1−p)→0, and ADR 0039's ridge is scaled by the very Fisher that collapses. A fixed L2 (head_l2, Hughes's lambda_w=0.001) finitizes |w|→4.76. An inner Newton loop (head_inner_iters) is FIXED-POINT-REDUNDANT with the fixed L2 — one aggregatable step per SVI iter accumulates to the same regularized MLE (INNER==one-step byte-for-byte; 1-step×60 == 60-step inner on a frozen design); the lever is the ridge TYPE not step count. The residual topics-LR gap to the full-batch L-BFGS reference (0.53 vs 0.87) is online/ALTERNATING vs JOINT optimization + shape-vs-regularize tension (the L2 that finitizes w damps shaping), not head convergence. Recorded as ADR 0040 (svi, pc, head-optimizer, prediction-constrained, case-finding)
- [0064](0064-lr-ranking-edge-yields-zero-fdr-discoveries-ranker-not-discoverer.md) — The LR readout's aggregate-ranking edge (+0.12 ROC / 2.7x PR-AUC over theta-mass) yields ZERO FDR-controlled discoveries: the same Efron two-groups empirical-null per-node FDR on the LR + explain-away score matrices gives n=0 at every q (0.05/0.10/0.20), identical to theta-mass. ROC is average case-level ranking; FDR asks whether any individual (patient,node) score survives BH correction vs the length-matched background null across ~31k patients — and at 4.5% prevalence with heavy overlap (LR bg_fpr 0.43 @ 80% sens, precision 0.08) no threshold reaches q-controlled precision with any recall. LR is a TRIAGE RANKER not a discoverer; the buried-signal problem is NOT a lens artifact at the discovery level, it is information-limited. Not a bug (planted test discovers on separated data). Resolves the LR-FDR un-chased lever (negative); only information/MixEHR remains (case-finding, fdr, lr-readout, discovery-vs-ranking, null-result, decision)
- [0063](0063-reverse-topo-spectral-init-is-null-init-geometry-washes-out.md) — Reverse-topological (leaves-first) spectral init is a NULL lever: indistinguishable from forward (ancestors-first) on detection (LR 0.779 vs 0.778), placement (mrr 0.590 vs 0.596), and the error classes (FN 276=276=276), because the DAG gate already supplies identifiability and 200 SVI iterations move off the init geometry. SIXTH consecutive model-side null (after fixed/learned alpha 0060/0061, explain-away + n_bg 0062). Completes the case-finding lever sweep: the single durable contribution is the LR READOUT LENS (+0.11-0.13 ROC over theta-mass), a readout change not a model change; binding constraint = INFORMATION (meds/labs = MixEHR). Feature kept (topo_order knob, forward default). Full collation: docs/reports/2026-07-23-case-finding-levers-retrospective.md (case-finding, spectral-init, topo-order, null-result, collation, decision)
- [0062](0062-explain-away-and-background-capacity-null-binding-constraint-is-information.md) — Explain-away (responsibility-weighted) routing AND doubled background capacity (n_bg 40→80) are BOTH null on case-finding, completing a three-lever sweep (learned alpha 0060/0061, explain-away, n_bg) that all fails. Decisive evidence = ERROR-CLASS totals not third-decimal AUC: FN class (rare→background) IDENTICAL under routing and under 2× background (276→276→276); dominant FP class (background→rare, ~13000 = 44% of background @80% sens) barely moves (13158→12992). Both classes reduce to the SAME diagnosis — INFORMATION not scoring/capacity: FN = data starvation (thin profiles), FP = GENUINE code-level similarity (a background patient with anemia+CKD is indistinguishable from SLE from condition codes, because those codes really are SLE features). Explain-away costs ~0.011 ROC (routing attenuates genuine positives too) and does NOT rescue its target FN class; n_bg=80 costs a full re-extract for noise. Plain LR @α→inf stays the durable readout (beats theta-mass +0.11-0.13 ROC). Binding constraint = multi-domain features (meds/labs = MixEHR). Explain-away kept as a validated viewer mode, not the default (case-finding, explain-away, n-bg, false-positive, information-constraint, null-result)
- [0061](0061-learned-per-node-alpha-null-on-detection-argmax-shift-not-settled.md) — The LEARNED per-node alpha (optimizeDocConcentration) confirms 0060 on real rare6 data: NULL on LR detection (0.777 vs symmetric 0.778). It SHIFTS cross-node argmax (mrr 0.585→0.442) but per-node discrimination + detection are UNCHANGED — the mechanical effect of a non-uniform node prior on fold-in argmax, confounded by single-seed multimodality (0059), measured on the WRONG objective (optimize_alpha maximizes held-out LL, not ranking; LL unmeasured). Placement NOT settled; only detection-null + no-demonstrated-benefit. Learned alpha is non-degenerate/interpretable (bg up 3.4× = uniform usage; disease nodes spread ~8×, mostly below bg = peaked). Keep optimize_alpha OFF in production (case-finding, optimize-alpha, learned-alpha, detection, null-result, decision)
- [0060](0060-fixed-block-asymmetric-alpha-is-a-null-lever-for-lookback-detection.md) — FIXED block-asymmetric alpha (node_alpha_scale 0.1, exps 0063/0064) is INDISTINGUISHABLE from symmetric (≤0.003) on every detection metric, both lenses (theta-mass, LR α→inf), both depths, both lookback windows. The forward-mode "asym helps" (0.709 vs 0.660) was an init/epochs confound that does NOT replicate on lookback. Mechanism: LR reads lambda directly and bypasses theta, so the theta-prior alpha barely moves detection. Predicted the learned alpha would also be null (confirmed by 0061). Binding constraint = information, not prior tuning (case-finding, optimize-alpha, detection, null-result)
- [0059](0059-gated-optimize-alpha-recovers-in-seed-ensemble-but-single-fits-are-multimodal.md) — The gated optimize_alpha (per-node tied asymmetric Dirichlet, exact dense gated Newton) recovers the planted alpha RANKING in a seed-ENSEMBLE (Spearman 1.0) but SINGLE-seed fits are MULTIMODAL — a node, even the highest, can crash to a wrong basin. Same seed-dependent-basins family as the PG-STM arc, now in the gated Dirichlet-LDA alpha. Math proven exact by finite-difference, so the scatter is optimization multimodality, not bias. Read gross structure (bg vs node blocks), not per-node points, from a single fit (case-finding, optimize-alpha, multimodality, seed-ensemble)
- [0058](0058-dag-offset-readout-coverage-failure-is-token-to-psi-inference-not-prior-or-ridge.md) — Localizes 0057's calibrated-coverage failure and REFUTES its ridge/prior hypothesis. Coverage-residual decomposition (well-specified plant: increments from the fit's OWN prior, Σ_true=I): Σ sampled → marg 0.06/joint 0.08, Σ PINNED at truth → 0.04/0.04 (pinning Σ changed nothing → not Σ-estimation; joint equally broken → not coordinate-frame; matched-prior 0.06 ≈ mismatched 0.00 → not prior scale — the earlier "matched→0.20" was R=10 noise). The estimate is right-scale but uncorrelated with truth (2×2 map R²=0.07, off-diagonal≈0 rules out a rotation) inside 6×-too-narrow intervals. DECISIVE CUT: feed the identical ridge the TRUE ψ field (planted closure-sums + true Σ=I noise) instead of token-inferred ψ → corr 0.996, B≈identity, posterior SD 0.100 = RMS miss 0.100 (calibration ratio 1.01). So compiler+ridge+MN-draw+schema+interval are all CORRECT and calibrated; the entire failure is upstream in token→ψ inference (each doc's fg-stick ψ is prior-dominated under ~80 tokens+gating). Two separated axes: RECOVERY is token-limited (node-3 R² −0.11→0.20→0.51 as doc_len 80→320→1280, toward oracle 0.99) but CALIBRATION is structurally overconfident (coverage ~0 even at R²=0.51 because the interval conditions on latent ψ as if observed, omitting ψ-inference variance — not token-fixable). CONSEQUENCE: Fable's τ-per-tier + joint τ+Σ hierarchy targets prior scale = refuted as the fix; do NOT build it on this evidence; the levers are token evidence (recovery) + propagating ψ-inference uncertainty into the coefficient posterior (calibration) (pg-stm, dag-offsets, read-out, coverage, calibration, gibbs, identifiability)
- [0057](0057-dag-offset-readout-recovers-ordering-but-fails-calibrated-coverage-under-exact-gibbs.md) — (refined by 0058) The DAG-offset read-out engine's coverage-plant acceptance gate returns an honest negative. The design-wall emit-but-flag contract HOLDS 12/12 (gauge/unresolved never emit a point estimate, recipe/convention present). But identified-coordinate coverage FAILS: (1) full-vector coverage is 0 BY CONSTRUCTION — a node's offset is reported over all K-1 sticks but identified only on its own group's sticks (insight 0054), so the off-block dims (prior-pinned ≈0) never cover a redrawn ~N(0,4) truth and the all-sticks np.all check can't pass → the read-out MUST restrict to each node's identified sub-block (insight 0053 rule as a schema requirement); (2) even on the node's OWN foreground stick coverage fails and it is NOT convergence — short chain 0.12, converged chain 0.40, persisting via mean ATTENUATION toward zero (truth 1.99→mean 0.76) + OVERCONFIDENT tight intervals + occasional sign-confound; recovery-correlation is positive (~0.6) so ORDERING works, only CALIBRATION fails. This REPRODUCES insight 0051 (order right / overconfident / coverage ~0.13) under the EXACT co-sampled Gibbs engine — moving VI→Gibbs did NOT cure calibration, exactly Fable's pre-registered branch where LKJ/half-t provenance priors + the ridge-attenuation fix must earn their place. Engine is BUILT + structure-validated (compiler/quotient/classification/design-wall/recovery all work); the calibrated-coverage gate is UNMET; the coverage test is xfail'd with a reason pointing here (threshold NOT loosened) (pg-stm, dag-offsets, read-out, coverage, calibration, gibbs)
- [0056](0056-anchor-level-confound-is-a-design-wall-under-exact-gibbs-not-an-estimator-artifact.md) — Fable's step-0 arbiter: is the anchor-level confound an ESTIMATOR wall (more data fixes it) or a DESIGN wall (no engine recovers it)? Built a faithful full Gibbs (the validated pg_stm_gibbs gated sweep with ONLY the Gamma point replaced by a proper matrix-normal offset DRAW C~MN(dag_offset_ridge, A^-1, Sigma), drawn+fed-back) on a K-reduced insight-0054 corpus over a 16x doc-count ladder. VERDICT = DESIGN WALL, decisively: (1) across 16x N only the A1 INCREMENT contracts (~1/sqrt(N), SD 3.16x) — every anchor LEVEL is FLAT (A 0.86x, B-level 0.85x, B/B1-split 0.96x); more data does not resolve them. (2) Analytic: the augmented design has nullity 2 (partition identity intercept==A+B confounds levels with the intercept; B1col==Bcol from no-direct-B-docs confounds the split), and BOTH null vectors load exactly zero on the A1-increment coordinate — the increment is the sole offset coordinate in the row space, exactly the one that contracted (Gibbs and linear algebra agree to the coordinate). (3) Design-wall directions are WIDE-BUT-STATIONARY (Fable outcome a, not b): improved_rhat on ||DIFF|| = 1.01 (proper prior, SD 8) and 1.00 (near-flat prior, SD 130), no wandering; width tracks the PRIOR exactly (8*sqrt(0.25/1e-3)~130) => the posterior along the wall IS the prior (data/N-independent). CONSEQUENCE: read-out engine acceptance must be PER-COORDINATE-CLASS (increments get a number+coverage; level/null directions report UNRESOLVED, a wide interval, never a point) — stationary so no export hole; the static compiler (closure_gram null + foreground_grams intercept-collinearity) is a valid pre-fit surrogate; the MN offset draw is the reusable step-2 kernel (pg-stm, dag-offsets, identifiability, gibbs, design-wall, read-out)
- [0055](0055-identifiability-compiler-validated-on-realistic-data-with-a-ridge-reparameterization-gap.md) — The v1 identifiability compiler (reads increment-identifiability off the closure Gram, rewrites the DAG to its identified quotient) VALIDATED on realistic-overlap DAG-offset corpora. Confirmed quantitatively: (1) closure_gram == the offset block of the fit's own XtX moment to machine zero ("if you can fit, you can compile"); (2) it reproduces the insight-0054 collapse deterministically (auto-merges the no-direct-docs anchor into its sole subtype, keeps identified structure, invariant exact) — what took a hand isolation-probe now emits from a count reduce; (3) the fragility spectrum is quantitative: d(anchor,subtype)==#distinguishing docs, collapse flips at strict d<tol, min eigenvalue grows monotone with own-docs (0->2.5->12->48) = the fragile->identified continuum; (4) safety split holds adversarially (cross-tree co-attestation FLAGGED not merged; deep chains collapse whole; no d<tol off-by-one). THE ONE SEAM: the quotient's merged node carries the identified sum only UP TO a depth-scaled-ridge reparameterization gap (merged node's single penalty vs the chain's two; tiny under light lam_base=1e-3, correlation ~1, but nonzero/systematic) — fix if the read-out engine needs the exact pre-collapse sum: set the merged penalty to match the chain (pg-stm, dag-offsets, identifiability, compiler, validation, ridge-reparameterization)

- [0054](0054-background-only-members-flip-the-identification-flag-but-anchor-levels-stay-intercept-confounded.md) — The gate-change payoff (DAG background-only member support, Task 4) is more nuanced than hoped. THREE findings: (1) recovery must be measured on FOREGROUND (node-specific) sticks — a node's offset on the SHARED background sticks is weakly identified, so correlating over all-active dims lets the noisy background dims dominate/sign-flip the aggregate (subtype A1: r(all-active)=+0.20 vs r(fg-only)=+0.89; the reparam r=0.76 was seed luck). (2) Background-only members flip the anchor identification FLAG (False->True, deterministic) but do NOT restore anchor-LEVEL point recovery: the anchor level stays confounded with the lightly-penalized global intercept ON THE GROUP FOREGROUND STICKS (bg-only docs never activate them), so the intercept absorbs the offset and B[u] shrinks toward 0 — a concrete instance of insight 0052 (ident flag flips while mean-field point estimate stays unrecovered). (3) A no-direct-docs single-child anchor has a residual node2+node4 collinearity that background grounding can't break (fg r -0.67 -> +0.93 only when given direct docs); the identified quantity is the subtype increment given BRANCHING evidence. USER INSIGHT: such a single-child, no-own-evidence node is COLLAPSIBLE into its child (tree case) — open wrinkle: a single-child node with MULTIPLE parents can't be trivially collapsed (offset shared across parent paths). Motivates a deferred ontology node-collapsing preprocessing step (pg-stm, dag-offsets, identifiability, background-only-members, ontology-structure, node-collapsing)
- [0053](0053-mean-zero-stick-breaking-under-attests-later-background-sticks.md) — Validating the flat background-only E-step (DAG background-only member support, Task 3): a pure background-only corpus generated from mean-0 stick-breaking systematically UNDER-attests the later background topics (E[θ₁]>E[θ₂]>…; measured signature-block token fractions ~[0.67,0.74,0.45,0.27,0.18]), so the 5th topic is weakly identified from the DATA regardless of estimator. Separately, single-word argmax is the wrong recovery measure under overlapping signatures — it is tie-break-brittle and lowering topic_overlap made it WORSE (probe 1/5 at overlap 0). Under the codebase-standard block-mass measure (planted_recovery), the flat E-step recovers every ADEQUATELY-ATTESTED topic: stable 4/5, per-planted best-fitted mass [0.97,0.90,0.86,0.63,0.42], the single miss being exactly the least-attested topic. The miss is an information/attestation property of the plant (insight 0052's information wall), NOT a flat-E-step bug; recovery MEASURE must isolate the estimator from the planted signal's identifiability (pg-stm, dag-offsets, stick-breaking, test-design, recovery-measure, information-wall)
- [0052](0052-two-engine-architecture-meanfield-is-sound-point-ordering-unsound-calibration.md) — SYNTHESIS (with Fable) of 0044/0047/0048/0051 into one theorem-shaped claim: all four share ONE mechanism — mean-field's KL(q‖p) makes q mode-seeking/under-dispersed, so (i) point estimates bias toward sharp/decoupled configs AND (ii) the self-reported variance measures scatter around the biased point, structurally BLIND to the bias. Clause (ii) = why no within-VI fix ever calibrates absolute uncertainty; the fix is the same everywhere = route absolute UQ through an exact-conditional (Gibbs) pass. → TWO-ENGINE ARCHITECTURE (measured division of labor): mean-field VI = sound point-AND-ORDERING engine (points + relative/ordinal signals trustworthy, e.g. 0051 width-ratio 2.0 design-moment); exact-conditional pass = the calibration engine. Also separates the INFORMATION wall (scarce-overlap unidentifiable from this data even by Gibbs → needs data/pooling, why the DAG exists) from the ESTIMATOR wall (info present, mean-field can't carry it → the engine climbs this; scarce blocks correctly come back WIDE). Paper-headline candidate (pg-stm, mean-field, calibration, uncertainty, two-engine, theory)
- [0051](0051-ridge-conditional-offset-intervals-order-uncertainty-but-are-overconfident-absolutely.md) — The v1 DAG-offset interval read-out (σ²·diag((WᵀW+diag(pen))⁻¹) at the VI ψ-mean) orders uncertainty correctly (scarce/populated width ratio 2.0, a design-moment property independent of σ²/n_iter) but is severely OVERconfident absolutely: a redraw-the-truth coverage probe measures ~0.13 vs nominal 0.90, systematically — even reps where the point estimate recovers well (r 0.73–0.82) cover only 0.11–0.17 (ridge sd ~10× too small). Cause: the variance is built from mean-field-biased ψ-means and is blind to that bias (measured against PLANTED truth). Ships as a RELATIVE read-out (width ordering only); calibrated absolute intervals need debiased inference → deferred to the read-out-honesty spec. Three-for-three with 0044/0047/0048: only relative/identified functionals are reportable from mean-field VI (pg-stm, dag-offsets, uncertainty, mean-field, calibration, coverage)
- [0050](0050-dag-anchor-offsets-are-unidentified-only-increments-carry-uncertainty-signal.md) — In the DAG additive-offset PG-STM, the individual ANCHOR offsets are un-identified under a partitioning gate: every doc belongs to exactly one anchor group so col_root ≡ col_A + col_B and intercept ≡ col_root (dummy-variable trap, two exact redundancies). The ridge splits the point estimate arbitrarily but the per-node DIAGONAL posterior variance is dominated by the shared unidentified direction — a 600-doc and a 60-doc anchor get identical interval widths (Ainv diag ≈ 249.94 both) → coverage 1.00, no scarce-wider signal. What IS identified is each node's parent→child INCREMENT (a subtype vs its anchor, given anchor-only docs break col_anchor ≡ Σ subtypes); its variance scales with the node's own doc count → scarce-wider is real there. The honest offset read-out is defined on increments, not anchor levels. Consequence for Task 4/6 of the model core (pg-stm, dag-offsets, identifiability, dummy-trap, ridge-posterior, coverage)
- [0049](0049-cosampling-recovers-well-populated-sigma-but-not-scarce-gated-foreground-under-overlap.md) — The Fable-round-3 pivot (VI warm-start + co-sampled (β,η,Σ) Gibbs) is PARTIALLY validated: naive co-sampling stays stuck in VI's sharp-β metastable mode (adj_cos 0.22, drift 0.80), but a smoother Gibbs β-prior (beta_eta=1.0) lets β relax (adj_cos 0.42) and the well-populated BACKGROUND block reaches oracle (MAE 0.136 vs 0.123); the data-scarce GATED FOREGROUND still fails (MAE 0.62 vs oracle 0.034) — the read-out failure lands exactly on the rare-subgroup case the gated model exists for. Side-findings: Gibbs Σ eigmin interior everywhere (Q3 caveat holds, floor was mean-field), no scale blowup (pg-stm, sigma-readout, co-sampling, warm-start, topic-overlap, gated, rare-subgroup)
- [0048](0048-condition-on-vi-beta-readout-is-bottlenecked-by-vi-beta-sharpening-under-topic-overlap.md) — On a realistic OVERLAPPING-topics corpus (adj_cos 0.58, peak word prob 0.036), the condition-on-VI-β Σ read-out FAILS: oracle-β Gibbs recovers the planted correlation (MAE 0.06) but VI's β is sharpened by mean-field (adj_cos 0.23-0.40 vs true 0.58) and conditioning on it corrupts Σ (bg MAE 0.37-0.45, scale explodes to 10 vs planted 4). The β-prior fix (beta_eta=1.0) restores overlap on a small corpus but backfires at real identification (likelihood dominates the prior). Freezing VI's β is not viable under overlap → pivot to co-sampled β warm-started from VI (pg-stm, sigma-readout, mean-field, beta-sharpening, topic-overlap, condition-on-beta)
- [0047](0047-the-sigma-eigmin-floor-is-a-mean-field-attenuation-artifact-gibbs-recovers-full-rank.md) — The at-scale Σ eigmin-floor is a MEAN-FIELD ATTENUATION ARTIFACT: on a full-rank well-conditioned synthetic truth (block PR 5.5, eigmin 2.8, cond 5), VI's fitted per-doc logit scatter collapses (PR 2.4, eigmin 2.8e-6, cond 2.5e6) while exact Gibbs with labels pinned recovers the truth (PR 6.7, eigmin 3.6, cond 7). Resolves 0046's open question; extends 0044 from correlation-sign to rank/conditioning/scale. Re-sequences the queue: low-rank Σ=ΛΛᵀ+D (item 3) would model the method not the data → demoted; the condition-on-VI-β Gibbs read-out (item 4) is the fix → promoted (pg-stm, sigma, mean-field, attenuation, rank, gibbs, condition-on-beta)
- [0046](0046-sigma-eigmin-floor-is-statistical-rank-deficiency-not-structural-to-the-gated-parameterization.md) — The at-scale Σ eigmin-at-floor (~1e-8) is NOT structural to the gated stick parameterization (Σ_true is well-conditioned, eigmin = eta_scale·(1−rho_grp), invariant to #groups) — it is statistical rank-deficiency in the fitted block scatter: rank-2-compressed scatter (an attenuation proxy) reproduces the exact 1e-8 MLE floor, while strong correlation alone can't (ρ=0.99 → 0.04). The IW ridge lifts the rank-deficient direction off the floor (2e-3 to 1e-1) — small-block shrinkage, a possible estimator contrast on conditioning the max|Σ| headline missed; low-rank Σ=ΛΛᵀ+D is the matched fix (pg-stm, sigma, eigmin, rank-deficiency, low-rank, inverse-wishart, null-space)
- [0045](0045-free-gibbs-sigma-readout-is-confounded-by-label-switching-not-weak-identification.md) — The unpinned free-Gibbs Σ read-out is confounded by topic label-switching (topics permute across chains, so per-stick R̂ is uninterpretable), NOT weak identification; conditioning on a fixed β pins labels (R̂→1, medians→truth) and the scarce-group scale is then wide-but-stationary under both weak and informative priors (no runaway resurfaces in exact Gibbs — refuting "the cure is only mean-field damping") (pg-stm, gibbs, sigma-readout, label-switching, mcmc-diagnostics, condition-on-beta)
- [0044](0044-meanfield-vi-fails-sigma-correlation-even-when-identified.md) — Mean-field VI reads the WRONG SIGN of the Σ correlation even on a stick-native corpus where Σ is identified and exact Gibbs recovers it; the comorbidity correlation read-out cannot use mean-field VI (needs exact Gibbs or a structured posterior); β/topic content is unaffected (pg-stm, vi, gibbs, sigma-correlation, mean-field)
- [0043](0043-permuted-null-presence-is-better-than-random-not-significantly-positive.md) — Permuted-topic "presence" (presence_vs_null) is a BETTER-THAN-RANDOM test, not a significantly-positive one: a random topic hurts held-out prediction (null-band mean < 0), so "beats the null" is a LOWER bar than "beats zero" — it inflates weak/background topics and rates a zero-gain reference topic 70% present (stm, diagnostics, predictive-gain, calibration)

- [0042](0042-cofit-beta-does-not-reproduce-stm-lda-peakiness-gap.md) — Co-fitting β does NOT reproduce the real STM-vs-LDA peakiness gap (the sign even flips: co-fit LDA is cooler than co-fit STM); LDA's Dirichlet pressure DOES carve a marginally sharper β, but a sharper β does not make patients peakier — so with 0038 (α ruled out) neither inference mechanism explains the real gap, which must be pinned on the real corpus (stm, lda, generation, concentration, beta-learning, calibration)
- [0041](0041-drug-anchored-active-comparator-gated-stm-recovers-drug-specific-structure.md) — A drug-anchored active-comparator gated STM (GLP-1 vs SGLT2i, exp 0044) separates drug-specific comorbidity (SGLT2i→cardiorenal, GLP-1→obesity+GI side effects) from the shared T2DM indication that lands in both arms; the drug-anchor track is a drop-in sibling of the disease track under the gate (stm, gating, cohorts, drug-anchor, pharmacoepi)
- [0040](0040-light-coder-years-are-mostly-routine-with-structured-pockets.md) — Light-coder (5–19-code) years are mostly routine/screening/acute-minor care with real MSK and metabolic pockets, justifying the doc_min_length floor without it being lossless; a foreground block's low NPMI (+0.10 vs +0.19) is the diffuse-short-doc signal, not a defect (stm, gating, cohorts, short-documents, npmi)
- [0039](0039-revisiting-0028-stabilized-gated-stm-recovers-rare-phenotypes.md) — Revisiting 0028: stabilized gated STM recovers rare/minority sub-phenotypes, so 0028's "prior family, not stabilization" verdict was confounded by missing stabilizers + fit-scale (stm, lda, gating, priors, rare-phenotypes, re-examination)
- [0038](0038-heldout-ll-recovers-true-concentration-and-lda-alpha-opt-is-not-hot.md) — Held-out predictive likelihood recovers the true per-document concentration (synthetic-validated, both prior families); LDA α-optimization does not read hot, so the STM-vs-LDA concentration gap is not an α-inference artifact (stm, lda, generation, concentration, calibration)
- [0037](0037-in-band-pooled-scale-converges-below-export-scale-faithful-scale-not-fit-recoverable.md) — The in-band pooled generative scale converges below the frozen-β export scale and both under-concentrate vs real per-patient θ; the faithful generative scale is not fit-recoverable (stm, generation, concentration, diagnostics)
- [0036](0036-gated-free-variance-runs-away-at-fit-but-not-at-export.md) — A free Σ variance runs away at FIT (even with reference + spectral) but a frozen-β pooled scale is bounded at EXPORT; the generative scale must be decoupled from the fitting prior (stm, generation, gating, diagnostics)
- [0035](0035-rare-disease-gated-foreground-recovers-eds-subphenotypes-on-full-population.md) — A rare-disease gated foreground (EDS, ~1k docs / ≈0.5%) recovers clinically faithful sub-phenotypes against a full-population background (stm, gating, cohorts, rare-disease)
- [0034](0034-blockwise-unit-diagonal-fixes-runaway-on-real-data-and-needs-a-correlation-clamp.md) — Block-wise unit-diagonal Σ fixes the variance runaway on real data (exp 27); per-cell standardization needs a −1..1 correlation clamp (stm, gating, conditioning, reporting)
- [0033](0033-gated-fullcov-variance-runaway-is-an-init-identifiability-failure.md) — The gated full-Σ variance runaway is an initialization / identifiability failure, not a thin-minority or model failure; unit-diagonal Σ is structural insurance (stm, conditioning, gating, diagnostics)
- [0032](0032-gated-fullcov-recovers-dementia-subphenotypes-and-exposes-spd-assembly-conditioning.md) — Gated full-covariance STM recovers Alzheimer's vs vascular dementia in the 19% minority arm; the predicted SPD-assembly ill-conditioning appears on real data and is gracefully handled (stm, full-covariance, gating, rare-phenotype, conditioning)
- [0031](0031-scalable-spectral-topic-quality-matches-dense-but-sigma-splits-one-runaway.md) — Scalable (random-projection) spectral init matches dense on topic quality but not Σ — a single dominant topic escapes into the blowup basin (stm, initialization, spectral, sigma)
- [0030](0030-spectral-init-closes-stm-sigma-blowup-on-real-data.md) — On real cancer data, spectral init brings STM's Σ from ~10^10 to ~7.6 and resolves all K topics at the default σ_init=1; the K−1 reference alone does not — the Σ blowup defeats the reference topic itself (stm, priors, svi, initialization, diagnostics, phenotyping)
- [0029](0029-stm-sigma-init-collapse-blowup-missing-stabilizers.md) — Our online STM's σ_init-selected collapse↔Σ-blowup (Σ→10^10) is a missing-stabilizer artifact, not a property of STM; published STM avoids it via spectral init + K−1 reference-topic identifiability + Σ shrinkage (stm, priors, svi, initialization, diagnostics, prior-art)
- [0028](0028-dirichlet-vs-logistic-normal-rare-phenotype-recovery-gated-lda-is-plda.md) — The prior family (Dirichlet vs logistic-normal), not gating, governs rare minority-phenotype recovery; STM collapses where LDA recovers (2x2 + repro); "gated LDA" is Partially Labeled Dirichlet Allocation (PLDA) (stm, lda, gating, priors, phenotyping, prior-art)
- [0027](0027-gated-stm-imbalanced-arms-majority-foreground-collapses.md) — Gated STM on imbalanced arms: the majority arm's foreground collapses into the shared background; the minority arm captures its anchor but not sub-phenotypes; per-block NPMI is non-comparable (stm, gating, covariates, npmi, doc-units, diagnostics)
- [0026](0026-stm-prevalence-gives-prevalence-not-content-fidelity.md) — Prevalence-only STM reproduces LDA's cohort concentration (prior-family-invariant) and gives rare covariate groups prevalence fidelity, not content fidelity (stm, lda, covariates, npmi, diagnostics)
- [0025](0025-min-patient-count-vs-min-df.md) — `min_patient_count` and `min_df` answer different questions (doc-units, diagnostics)
- [0024](0024-labeler-classifier-rules-have-regime-dependent-blind-spots.md) — LLM-classifier rubrics have regime-dependent blind spots: rules that look robust on the development corpus can mis-fire when the feature distribution shifts (ops, diagnostics, labeling)
- [0023](0023-producer-consumer-unit-mismatches-invisible-until-small-scale.md) — Dimensional unit mismatches in producer/consumer pairs are invisible at large input scale; small cohorts expose them (ops, diagnostics)
- [0022](0022-phenotype-vocabulary-refines-past-elbo-plateau.md) — Topic-word concentration on small-α tail topics continues refining well past mass-distribution convergence (lda, svi, diagnostics)
- [0021](0021-cohort-corpora-two-anchor-mass-concentration.md) — Cohort-filtered corpora concentrate 90%+ of mass into two universal-anchor topics regardless of K (lda, doc-units, diagnostics)
- [0020](0020-small-cohort-worker-count-reversal.md) — Below ~1k docs/worker/epoch, fewer Spark workers wins on wall time (ops, svi)
- [0019](0019-lda-large-k-with-full-convergence-gracefully-unused-slots.md) — LDA at large K with full convergence gracefully under-uses excess capacity, no micro-cluster artifacts (lda, doc-units, diagnostics, hdp)
- [0018](0018-full-corpus-plus-threshold-yields-unimodal-positive-npmi.md) — Full-corpus reference + min-pair-count threshold yields unimodal positive NPMI distribution (npmi, diagnostics, hdp)
- [0017](0017-hdp-gamma-sensitivity-is-prior-dominance.md) — HDP γ-sensitivity reveals prior-dominated outcomes (hdp, diagnostics)
- [0016](0016-condition-era-vs-occurrence-not-comparable.md) — condition_era and condition_occurrence runs are not directly comparable (doc-units, npmi, diagnostics)
- [0015](0015-crisp-topics-can-regress-when-k-undersized.md) — Crisp topics can regress in late iters when K is undersized (lda, diagnostics)
- [0014](0014-patient-year-npmi-bimodal-vs-lifetime-unimodal.md) — Patient-year LDA NPMI is bimodal; patient-lifetime is unimodal (lda, doc-units, npmi)
- [0013](0013-spark-scaling-driver-bottleneck.md) — Spark executor count past ~20 doesn't reduce iter time (ops, svi)
- [0012](0012-svi-batch-fraction-vs-iter-count-tradeoff.md) — SVI batch-fraction and iter count must be tuned together (svi)
- [0011](0011-min-doc-length-is-phenotype-vs-noise-tradeoff.md) — min_doc_length is a phenotype-vs-noise trade-off (doc-units, diagnostics)
- [0010](0010-npmi-not-comparable-across-doc-units.md) — NPMI absolute values are not comparable across doc units (npmi, doc-units)
- [0009](0009-year-binning-intensifies-chronic-bg-for-hdp.md) — Year-binning intensifies chronic-background dominance for HDP (doc-units, hdp)
- [0008](0008-patient-year-docs-surface-transient-phenotypes.md) — Patient-year docs surface transient phenotypes that lifetime docs smear (doc-units, lda)
- [0007](0007-npmi-zero-pair-floor-penalizes-rare-phenotypes.md) — NPMI floors at −1 for zero-pair counts, penalizing rare phenotypes (npmi, diagnostics)
- [0006](0006-spread-and-eff-k-interpretation.md) — λ-spread and effective-K must be read together (diagnostics)
- [0005](0005-lda-decomposes-background-into-flavors.md) — LDA decomposes "background" into multiple flavors on patient-year docs (lda, doc-units)
- [0004](0004-lda-asymmetric-alpha-settles-late.md) — LDA asymmetric α settles later than topic-word distributions (lda, diagnostics)
- [0003](0003-hdp-vs-lda-per-iter-cost-on-short-docs.md) — HDP per-iter cost grows faster than LDA on short documents (hdp, svi, ops)
- [0002](0002-hdp-catchall-hoarding-at-last-stick.md) — HDP catch-all hoarding at the last stick (hdp)
- [0001](0001-hdp-gamma-collapse-at-low-gamma0.md) — HDP γ-collapse at low γ₀ (hdp)
